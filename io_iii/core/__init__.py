"""
IO-III core package.

This namespace contains the deterministic execution kernel (code),
distinct from architecture/runtime/ which contains runtime artefacts
(configuration, invariants, and operational scripts).

Phase 2: structural consolidation only (no behavioural change).
"""